kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: hazelcast-pod-pod
spec:
  podSelector:
    matchLabels:
      role: hazelcast
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              project: user-amo-hazelcasttest
          podSelector:
            matchLabels:
              role: hazelcast